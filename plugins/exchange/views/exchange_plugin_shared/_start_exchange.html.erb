<% element_id = nil unless defined?(element_id) %>
<% element_type = nil unless defined?(element_type) %>

<% if logged_in? %>
  <% url = {:controller => :exchange_plugin_profile, :action => :start_exchange, :profile => profile.identifier, :element_id => element_id, :element_type => element_type} %>
<% else %>
  <% url = {:controller => :account, :action => :signup} %>
<% end %>
<%= link_to _('Start an exchange'), url, :class => 'exchange' %>
