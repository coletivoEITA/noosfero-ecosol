type = params[:object_type].gsub('::', '').underscore

page.remove "exchange-#{type}-element-#{params[:object_id]}"
page.insert_html :bottom, "exchange-#{type}-#{params[:actor]}-list", :partial => "remove_element",
:locals => {:element => @element, :actor => params[:actor], :proposal_id => params[:proposal_id]}

