type = params[:element_type].gsub('::', '').underscore

page.remove "exchange-#{type}-element-#{params[:element_id]}"
page.insert_html :bottom, "exchange-#{type}-#{params[:enterprise]}-list", :partial => "remove_element",
:locals => {:element => @element, :enterprise => params[:enterprise], :proposal_id => params[:proposal_id]}

