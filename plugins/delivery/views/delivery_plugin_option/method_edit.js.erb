<% if !request.put? %>
  jQuery('#delivery-method-edit').html("<%= escape_javascript(render partial: 'method_edit', locals: {delivery_method: @delivery_method}) %>");
  delivery.view_toggle();
<% else %>
  jQuery('#select-content').html("<%= escape_javascript(render partial: 'select_content') %>");
<% end %>
