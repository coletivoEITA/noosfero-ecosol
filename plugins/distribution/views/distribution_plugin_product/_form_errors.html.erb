<% if form_errors.errors.any? %>
  <div>
    <strong><%= _('Errors found:') %></strong>
    <ul>
      <% form_errors.errors.each do |attr,message| %>
        <li><%= "#{_(attr.humanize)} #{message}" %></li>
      <% end %>
    </ul>
  </div>
<% end %>

