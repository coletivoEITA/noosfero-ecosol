  <%= pagination_links @products %>
<div id="result-count" class="box-align">
  <small><%= t('distribution_plugin.views.product._search.showing_pcount_produc') % {:pcount => @products.length, :allpcount => @all_products_count} %></small>
</div>

<div id="results" class="box-align">
  <div class="table-header" style="<%= "display: none" if @products.empty? %>">
    <div class="box-field status"><%=t('distribution_plugin.views.product._search.status')%></div>
    <div class="box-field category"><%=t('distribution_plugin.views.product._search.category')%></div>
    <div class="box-field supplier"><%=t('distribution_plugin.views.product._search.supplier')%></div>
    <div class="box-field product"><%=t('distribution_plugin.views.product._search.product')%></div>
    <div class="box-field price-with-unit"><%=t('distribution_plugin.views.product._search.price')%></div>
    <div class="box-field stock"><%=t('distribution_plugin.views.product._search.qty_avail')%></div>
    <div class="cleaner"></div>
  </div>

  <div class="table-content">
    <div id="our-product-add" class="our-product value-row" toggleedit="distribution.our_product_toggle_edit();" style="display: none">
      <%= render :partial => "edit", :locals => {:product => @new_product} %>
    </div>

    <% if @products.empty? %>
      <%=  t('distribution_plugin.views.product._search.we_don_t_have_product') %>
    <% else %>
      <% @products.each do |product| %>
        <div id="our-product-<%=product.id%>" class="our-product value-row" toggleedit="distribution.our_product_toggle_edit();">
          <%= render :partial => "edit_line", :locals => {:product => product} %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
