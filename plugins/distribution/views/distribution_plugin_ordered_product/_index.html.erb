<div class="order-products-container <%= "empty" if order.products.count == 0 %>">
  <% if order.products.count > 0 %>
    <div class="order-products">
      <% f.fields_for :products, order.products do |f| %>
        <% order.products.each do |product| %>
          <%= render :partial => "distribution_plugin_ordered_product/edit", :locals => {:ordered_product => product, :product => product.offered_product, :f => f} %>
        <% end %>
      <% end %>
    </div>

    <div id="order-total-<%=order.id%>" class="order-total">
      <%= render :partial => 'distribution_plugin_order/total', :locals => {:order => order} %>
    </div>
  <% else %>
    <%= t('distribution_plugin.views.ordered_product._index.empty_order') %>
  <% end %>

  <div class="cleaner"></div>
</div>

