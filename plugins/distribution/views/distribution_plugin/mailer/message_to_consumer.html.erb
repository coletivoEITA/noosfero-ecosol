<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
  </head>
  <body>
    <p>
      <%= _('Hello %{name},') % {:name => @consumer.name} %>
    </p>

    <p><%= _('The collective %{node} sent a messsage to you') % {:node => @node.name} %></p>
    <p><i><%= @message %></i></p>

    <p>
      --<br/>
    </p>

    <small style="color: #888"><%= _('A service of %s.') % @environment.name %></small>
  </body>
</html>
