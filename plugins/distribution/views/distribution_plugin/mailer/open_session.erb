<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
  </head>
  <body>
    <p>
      <%= t('distribution_plugin.views.mailer.open_session.hello_member_of_name') % {:name => @node.name} %>
    </p>

    <p><%= t('distribution_plugin.views.mailer.open_session.a_new_cycle_is_open_c') + @session.name %></p>
    <p><%= t('distribution_plugin.views.mailer.open_session.the_cycle_description') %></p>
    <p><%= @session.description %></p>
    <p><%= t('distribution_plugin.views.mailer.open_session.the_administrator_let') %></p>
    <p><%= @message %></p>

    <p>
      --<br/>
    </p>

    <small style="color: #888"><%= @environment.name %></small>
  </body>
</html>

