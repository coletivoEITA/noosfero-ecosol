<% extend DistributionPlugin::DistributionDisplayHelper %>

<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
  </head>

  <body>
    <p>
      <%= t('distribution_plugin.views.mailer.order_cancellation.dear_name') % {:name => @consumer.name} %>
    </p>

    <p>
      <%= t('distribution_plugin.views.mailer.order_cancellation.your_order_made_on_co') % {
        :node => @node.name, :date => day_time(@order.updated_at)
      } %>
    </p>

    <p>
      --<br/>
      <%= t('distribution_plugin.views.mailer.order_cancellation.greetings') %>
    </p>
  </body>
</html>


