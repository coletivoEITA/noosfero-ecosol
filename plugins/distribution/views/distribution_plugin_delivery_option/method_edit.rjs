page.replace_html 'delivery-method-new', :partial => 'method_edit', :locals => {:delivery_method => @delivery_method} 
if !request.post?
  page.replace_html 'select-content', :partial => 'select_content'
  page << 'distribution_delivery_view_toggle()'
end
