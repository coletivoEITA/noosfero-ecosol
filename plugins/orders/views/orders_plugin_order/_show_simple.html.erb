<% actor_name ||= :consumer %>
<div id="order-<%=order.id%>" class="order-view">
  <% @simple = true %>
  <div class="order-data">
    <%= render "orders_plugin_item/index", order: order, actor_name: actor_name %>
    <%= render 'orders_plugin_order/delivery', order: order,
      actor_name: :consumer %>
    <%= render 'orders_plugin_order/delivery', order: order,
      actor_name: :supplier %>
  </div>
</div>
