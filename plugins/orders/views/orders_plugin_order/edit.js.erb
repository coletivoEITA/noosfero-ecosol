jQuery('#consumer-order-<%=@order.id%>').html(<%= render('orders_cycle_plugin_order/active_order', order: @order, actor_name: @actor_name).to_json %>);

<% if notice = session.delete(:notice) %>
  display_notice(<%= notice.to_json %>)
<% end %>

