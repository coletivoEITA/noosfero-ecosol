<% month ||= nil %>
<% year ||= nil %>
<% context_id ||= nil %>

<%= render 'orders_plugin_admin/filter', :actor_name => :consumer, :actors => actors, :orders_method => :sales, :orders_context => orders_context, :context_id => context_id, :month => month, :year => year %>
<%= render 'orders_plugin_admin/actions' %>

<div id='sales-table'>
  <%= render 'orders_plugin_admin/table', :actor_name => :consumer, :orders => orders %>
</div>

