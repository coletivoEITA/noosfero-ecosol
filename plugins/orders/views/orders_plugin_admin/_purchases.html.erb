<% month ||= nil %>
<% year ||= nil %>
<% context_id ||= nil %>

<%= render 'orders_plugin_admin/filter', :actor_name => :supplier, :actors => actors, :orders_method => :purchases, :orders_context => orders_context, :context_id => context_id, :year => year, :month => month %>
<%= render 'orders_plugin_admin/actions' %>

<div id='purchases-table'>
  <%= render 'orders_plugin_admin/table', :actor_name => :supplier, :orders => orders %>
</div>

