<% profile = knowledge.profile %>

<div class="escambo-snippet cms-learning-plugin-learning-snippet">
  <div class="first-column">
    <div class="label">
      <%= _('Learning') %>
    </div>
  </div>

  <div class="second-column">
    <%= _('%{profile} knows %{learning}') % {
      :profile => link_to(profile.name, profile.url),
      :learning => link_to(knowledge.name, knowledge.url),
      } %>

    <div class="description">
      <%= knowledge.summary %>
    </div>

    <%= render :partial => 'snippets/local', :locals => {:profile => profile} %>
  </div>

  <div class="third-column">
  </div>

  <% if profile != @active_organization %>
    <%= render :partial => 'exchange_plugin_shared/start_exchange', :locals => {:profile => profile} %>
  <% end %>

  <div class="clean"></div>
</div>
