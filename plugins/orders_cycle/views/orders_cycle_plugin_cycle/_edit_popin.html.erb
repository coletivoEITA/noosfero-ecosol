<div class="consumers-coop-plugin-popin">
  <h1><%= t('views.cycle._edit_popin.cycle_editing') %></h1>

  <div>
    <% if cycle.valid? %>
      <%= t('views.cycle._edit_popin.cycle_saved') %>
    <% else %>
      <% cycle.errors.each_full do |msg| %>
        <div><%= msg %></div>
      <% end %>
    <% end %>
  </div>

  <div>
    <%= colorbox_close_button t('views.cycle._edit_popin.close') %>
  </div>

</div>
