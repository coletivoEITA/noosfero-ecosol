<% fields_for :email do |f| %>
  <%= labelled_field f, :subject, t('consumers_coop_plugin.views.message._fields.subject'), f.text_field(:subject) %>
  <%= labelled_field f, :message, t('consumers_coop_plugin.views.message._fields.message'), f.text_area(:message) %>
<% end %>
